[package]
name = "flowgrid-device"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "FLOWGRID_DEVICE parsing for Flowgrid LLM preview binaries (no Burn dependency)."

[dependencies]
